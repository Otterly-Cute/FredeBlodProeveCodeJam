using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Generics : MonoBehaviour
{
/*

    //records,  den laver classen for en
    public sealed record GenericClass<T>(T Item);

    

 
    void Start()
    {
        //generic kan tage flere forskellige slags variabler.
        var x = new GenericClass<int>(Item: 1);
        var y = new GenericClass<string>(Item: "hello");
        var z = new GenericClass<int>(Item: 2);
        //de kan ikke skifte type bagefter

        GenericMethod(items: new List<int>() { 1, 2, 3, 4 });
        GenericMethod(items: new List<string>() { "hello", "i'm", "Clara" });



    }

    //ved en generic method vi kan bruge forskellige typer variabler uden at skulle lave flere methods for hver slags (fx. en for strings og en for ints)
    public static void GenericMethod<T>(List<T> items)
    {
        foreach (var item in items) 
            {
                Console.WriteLine($"Item: {item}");
            }
    }


    

    */
   
    
}
